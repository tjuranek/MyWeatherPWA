/** @jsx jsx */
import { jsx } from '@emotion/core';

export const LocationsList = props => {
	const { handleAddLocation, handleDeleteLocation, locations } = props;

	// WHERE I LEFT OFF
	// i need to make the container suppor thte list item and creating a list with the add location form
	// after this is done I need to add react router and be able to change routes
	// then i need to add a menu with two buttons, changing the route with each
};
